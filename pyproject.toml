[project]
name = "comfyui-prometheus-exporter"
version = "1.0.0"
description = "Prometheus /metrics exporter for ComfyUI"
license = { file = "LICENSE" }
requires-python = ">=3.10"
readme = "README.md"
dependencies = ["prometheus-client"]

[tool.comfy]
PublisherId = "druggeri"
DisplayName = "ComfyUI Prometheus Exporter"
